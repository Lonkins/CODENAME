# Internal working documents — never committed
CLAUDE.md
.internal/

# macOS
.DS_Store

# Xcode / SwiftPM
.build/
build/
DerivedData/
xcuserdata/
*.xcuserstate

# User-supplied content (BIOS/firmware/ROMs) — never committed
*.bin
*.rom
*.7z
/System/
/BIOS/

# Signing material — never committed
*.p12
*.mobileprovision
*.provisionprofile
*.pem
*.key
